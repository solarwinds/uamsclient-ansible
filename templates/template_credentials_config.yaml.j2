# Example credentials configuration file used with managed locally Agent
{% if access_key_id is defined %}
credentials:
  aws-secrets-manager:
    auth:
      type: static
      access-key-id: {{ access_key_id }}
      secret-access-key: {{ secret_access_key }}
      region: us-east-1
{% endif %}
